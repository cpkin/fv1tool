---
phase: 03-audio-interaction-export
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/ui/LoopRegion.tsx
  - src/ui/WaveformDisplay.tsx
  - src/audio/playbackManager.ts
  - src/store/playbackStore.ts
  - src/styles/app.css
autonomous: true

must_haves:
  truths:
    - "User can click waveform to jump to that position"
    - "User can toggle loop mode on/off"
    - "User can drag loop start/end points via overlay handles"
    - "Playback loops within selected region when loop enabled"
  artifacts:
    - path: "src/ui/LoopRegion.tsx"
      provides: "Draggable loop start/end handles"
      min_lines: 100
    - path: "src/ui/WaveformDisplay.tsx"
      provides: "Click-to-seek handler (updated)"
      contains: "handleWaveformClick"
    - path: "src/audio/playbackManager.ts"
      provides: "Loop region management (updated)"
      contains: "setLoopRegion|loopStart|loopEnd"
  key_links:
    - from: "src/ui/LoopRegion.tsx"
      to: "src/audio/playbackManager.ts"
      via: "setLoopRegion method"
      pattern: "playbackManager\\.setLoopRegion"
    - from: "src/ui/WaveformDisplay.tsx"
      to: "src/audio/playbackManager.ts"
      via: "seek method on click"
      pattern: "playbackManager\\.seek"
    - from: "src/audio/playbackManager.ts"
      to: "AudioBufferSourceNode"
      via: "loop properties"
      pattern: "source\\.loop|source\\.loopStart|source\\.loopEnd"
---

<objective>
Add interactive waveform scrubbing and loop region controls for precise playback manipulation.

Purpose: Users need to quickly audition specific sections of rendered audio and create loops to hear how effects evolve over time. Scrubbing enables fast navigation, and loop regions enable focused listening.

Output: Click-to-seek on waveform, draggable loop region markers, loop toggle button, and looping playback behavior.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-audio-interaction-export/03-CONTEXT.md
@.planning/phases/03-audio-interaction-export/03-RESEARCH.md
@.planning/phases/03-audio-interaction-export/03-01-SUMMARY.md
@src/audio/playbackManager.ts
@src/ui/WaveformDisplay.tsx
@src/store/playbackStore.ts
</context>

<tasks>

<task type="auto">
  <name>Add waveform click-to-seek</name>
  <files>
    src/ui/WaveformDisplay.tsx
    src/audio/playbackManager.ts
  </files>
  <action>
Enable click-to-seek on waveform canvas:

1. In WaveformDisplay.tsx:
   - Add onClick handler to canvas element
   - Calculate click position: event.clientX - canvas.getBoundingClientRect().left
   - Convert to time: (clickX / canvas.width) * buffer.duration
   - Call playbackManager.seek(time)

2. Update playbackManager.ts seek() method:
   - Store new time in pausedAt
   - If currently playing: stop current source, create new source starting at pausedAt
   - If paused: just update pausedAt (next play() will start there)
   - Update playbackStore.playheadTime to new time

3. Add visual feedback:
   - Change cursor to pointer when hovering over waveform canvas
   - Brief highlight effect on click (optional - Claude's discretion)

Handle edge cases:
- Clamp seek time to [0, duration]
- If buffer is null, ignore clicks
  </action>
  <verify>
npm run dev
Render audio, click waveform at different positions
Check: playback jumps to clicked position immediately
Check: clicking while paused updates cursor position
Check: clicking while playing restarts from new position
  </verify>
  <done>
Clicking anywhere on waveform seeks playback to that position, cursor updates immediately, audio continues playing from new position if it was playing
  </done>
</task>

<task type="auto">
  <name>Build loop region component with draggable handles</name>
  <files>
    src/ui/LoopRegion.tsx
    src/store/playbackStore.ts
    src/audio/playbackManager.ts
    src/styles/app.css
  </files>
  <action>
Create LoopRegion.tsx overlay for waveform with draggable handles:

1. Component props: duration (number), width (number), height (number)
2. State in playbackStore: loopStart (number), loopEnd (number), isLooping (boolean)
3. Actions: setLoopRegion(start, end), setIsLooping(enabled)

4. Render loop region visualization:
   - Shaded background between loopStart and loopEnd (rgba(59, 130, 246, 0.15))
   - Left handle at loopStart position (vertical bar with drag indicator)
   - Right handle at loopEnd position (vertical bar with drag indicator)
   - Position handles: x = (time / duration) * width

5. Implement drag handlers for start/end point adjustment:
   - onMouseDown on handle: set dragging state ('start' | 'end')
   - onMouseMove (window): calculate new time from mouse x position, update loop region
   - onMouseUp (window): clear dragging state
   - Validate: loopStart < loopEnd (maintain minimum 0.1s gap)
   - Clamp to [0, duration]
   - Implementation note: Overlay handles provide drag interaction for start/end points (not waveform-integrated markers)

6. Call playbackManager.setLoopRegion(start, end) when user drags handles

7. Update playbackManager.ts:
   - Add fields: loopStart (default 0), loopEnd (default duration), isLooping (default false)
   - Add setLoopRegion(start: number, end: number) method
   - Add setLooping(enabled: boolean) method
   - In play(): set source.loop = this.isLooping, source.loopStart = this.loopStart, source.loopEnd = this.loopEnd

Style in app.css:
- Handles: 4px wide vertical bars, bright blue (#3b82f6), hover cursor grab
- Dragging cursor: grabbing
- Loop region background subtle blue tint
  </action>
  <verify>
npm run dev
Render audio, drag left loop handle
Check: handle moves with mouse, loop start time updates
Drag right handle: moves independently
Check: handles cannot cross (loopStart < loopEnd enforced)
Enable loop toggle, click play
Check: audio loops within marked region
  </verify>
  <done>
Loop region displays on waveform with draggable start/end handles, handles move smoothly with mouse, loop region clamped to valid range, dragging updates playbackManager loop bounds
  </done>
</task>

<task type="auto">
  <name>Add loop toggle and wire into UI</name>
  <files>
    src/ui/PlaybackControls.tsx
    src/ui/WaveformDisplay.tsx
    src/App.tsx
  </files>
  <action>
Add loop toggle control and integrate loop region into waveform:

1. In PlaybackControls.tsx:
   - Add loop toggle button next to play/pause
   - Button shows loop icon (🔁 or similar) with on/off state
   - Clicking toggles playbackStore.isLooping
   - Calls playbackManager.setLooping(newState)
   - Visual state: highlighted when loop enabled, dim when disabled

2. In WaveformDisplay.tsx (or create wrapper component):
   - Position LoopRegion as absolute overlay on waveform canvas
   - Pass canvas dimensions and buffer duration as props
   - Show LoopRegion only when buffer exists
   - Z-index ensures handles appear above waveform but below cursor

3. Initialize loop region on buffer load:
   - When new buffer set: setLoopRegion(0, buffer.duration)
   - Reset isLooping to false

Layout:
- Loop toggle button next to play/pause (horizontal layout)
- LoopRegion overlays waveform without obscuring waveform detail

Handle interaction priority:
- Loop handle drag takes precedence over waveform click-to-seek (check event target)
  </action>
  <verify>
npm run dev
Render audio
Check: loop region handles appear on waveform edges (start=0, end=duration)
Drag handles to select middle section
Enable loop toggle (button highlighted)
Click play
Check: audio loops within selected region, does not play beyond end marker
Disable loop toggle
Check: audio plays through to end without looping
  </verify>
  <done>
Loop toggle button appears in PlaybackControls, clicking enables/disables loop mode, loop region displays as overlay on waveform, audio playback respects loop boundaries when loop enabled, entire track plays when loop disabled
  </done>
</task>

</tasks>

<verification>
Manual verification checks:

1. Click-to-seek:
   - npm run dev, render audio
   - Click at 25% position on waveform: playback jumps to 25% of duration
   - Click at 75%: jumps to 75%
   - Click while paused: cursor updates without starting playback
   - Click while playing: continues playing from new position

2. Loop region dragging:
   - Drag left handle from 0% to 20%: handle moves smoothly
   - Drag right handle from 100% to 60%: handle moves smoothly
   - Try to drag handles past each other: enforces minimum gap

3. Loop playback:
   - Set loop region to middle 50% of audio
   - Enable loop toggle (button highlighted)
   - Click play: audio loops within region, never plays beyond markers
   - Disable loop toggle: audio plays to end
   - Re-enable while playing: immediately starts looping

4. Edge cases:
   - Drag handle outside canvas: clamped to [0, duration]
   - Click waveform inside loop region: seeks correctly (not blocked by overlay)
</verification>

<success_criteria>
1. Clicking waveform seeks playback to clicked position immediately
2. Loop region displays with draggable start and end handles on waveform
3. Dragging handles updates loop boundaries, enforces loopStart < loopEnd with 0.1s minimum gap
4. Loop toggle button enables/disables loop mode with clear visual state
5. Audio playback loops within selected region when loop mode enabled
6. Audio plays entire track when loop mode disabled
7. Loop region resets to [0, duration] when new audio is rendered
</success_criteria>

<output>
After completion, create `.planning/phases/03-audio-interaction-export/03-02-SUMMARY.md`
</output>
